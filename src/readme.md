### Вводные по параметрам система
1. сколько пользователей в системе? Какой масштаб планируется через 3 месяца, полгода, год?
ОТВЕТ: Пользователей 100тыс, через год планируем 1млн;

2. какой РПС для входящих событий сейчас?
ОТВЕТ: 5-20 запросов в секунду;

3. сколько новых видов событий мы потенциально можем получить в ближайший год-два: это пара десятков или пара сотен?
ОТВЕТ: сейчас 4 вида событий, планируем до 20шт через год;

4. источники событий. нам в ручку стучатся с наших апок/веб-сайта или там же интеграции с банками и другими сервисами?
ОТВЕТ: все стучатся в одну ручку;

5. ~~могу ли я добавить идентификатор иденмпотентности для входящий событий (в структуре его нет)? Или можно считать, что входящие события 100% не будут дублироваться?
ОТВЕТ: потери и дубли возможны, можно считать что есть event_id + source для иденмпотентности;~~
УТОЧНЕНИЕ: собрал токен идемпотентности из того что было: время, тип и айди пользователя.

6. возможно ли нарушения порядка или потеря входящий событий?
ОТВЕТ: да

7. нужна ли возможность изменения набора правил без релиза? аля через админку
ОТВЕТ: изменений правил без релиза в данный момент не обязательно;

8. дедупликация. user_id+template+day - достаточно? или еще учитываем и канал?
ОТВЕТ: достаточно только user_id+template или user_id+template+day, если дубли разрешены раз в Х секунд;

9. Если в audit будем выдавать последние 100 ивентов, ок? и дадим фильм по дате
ОТВЕТ: 100 событий достаточно, фильтр по дню - ок
УТОЧНЕНИЕ: последние 100 событий, без фильтра по дню (без БД он слишком простой)

10. куда слать HIGH_RISK_ALERT? если для упрощения сделать CRITICAL лог - достаточно? можно алерт в телеграм группе
ОТВЕТ: в текущей версии можно в логи

Дополнительно следующие MVP-ограничения:
11. Без БД, храним в памяти;
12. Считаем что везде UTC;
13. ~~Без event_id+source считаем 400ой;~~ Незнакомые события или кривые тоже отвечаем 400ой;
14. Без auth, rate limit, ретраев, очередей и без метрик;

### Осознанные упрощения:
1. В тестах нет моков констант, хотя код самой бизнес логики рассчитан на их изменение;
2. В коде 4х стратегий и входящих/исходящих событий встречается копипаст кода. Имхо, в рамках тестового задания, чтобы было легко проверить и ревьюер не махнул руков на всю эту "микро-оптимизацию" лучше умеренный копипаст код, нежели крошечные методы/классы в несколько строк;
3. Весь код синхронный, т.к. нет работы с сетью или диском (вместо БД - dict);
4. Очень хотелось настроить mypy, но руки не дошли, поэтому могут быть какие-то нестыковки в типизации или импортах, НО на логику это не должно повлиять;
5. В тестах на бизнес-логику стратегий активно используется вызов крон-скрипта. Хотя это некоторая денормализация, так существенно проще.
6. В create-ручке явно вызывается крон-скрипт, чтобы не ждать 1 минуту, когда он правда отработает.

# Как запустить
1. Установить зависимости: `pip install -r req.txt`
2. Запустить сервер: `uvicorn src.main:app --reload`
3. curl для добавления ивента `signup_completed`:
```bash
curl -X POST "http://localhost:8000/api/external/event_in/create"   -H "Content-Type: application/json"   -d '{
        "user_id": "u_12345",
        "event_type": "signup_completed",
        "event_timestamp": "2025-10-31T19:22:11Z",
        "user_traits": {
            "email": "maria@example.com",
            "country": "PT",
            "marketing_opt_in": true,
            "risk_segment": "MEDIUM"
        }
      }'
```
4. curl для проверки, что был создан входящий и исходящий ивент:
```bash
curl -X GET "http://localhost:8000/api/admin/user/audit/u_12345"      -H "Accept: application/json"
```
5. curl для добавления ивента `payment_failed` (можно отправить 2ой раз, чтобы убедиться, что идемпотентность работает):
```bash
curl -X POST "http://localhost:8000/api/external/event_in/create"   -H "Content-Type: application/json"   -d '{
        "user_id": "u_12345",
        "event_type": "payment_failed",
        "event_timestamp": "2025-10-31T19:24:11Z",
        "properties": {
            "amount": 1425.00,
            "attempt_number": 2,
            "failure_reason": "INSUFFICIENT_FUNDS"
        },
        "user_traits": {
            "email": "maria@example.com",
            "country": "PT",
            "marketing_opt_in": true,
            "risk_segment": "MEDIUM"
        }
      }'
```
#  Есть тесты, рекомендую посмотреть!
Особенно качественно покрыта логика 4 стратегий и ручки создания пользователя. Можно найти разные корнер-кейс в духе "2 проваленных оплаты, 1 успешная, 2 проваленных => не хай риск"